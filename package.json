{
  "name": "contented-ui",
  "version": "1.0.0",
  "description": "a satisfying uPortal content experience",
  "main": "index.js",
  "author": "Christian Murphy <cmurphy@unicon.net>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "serve .",
    "precommit": "prettier --write **.*{css,js,json,md}",
    "gh-pages": "git branch -D gh-pages && git checkout -b gh-pages && rm .gitignore && yarn --production && git commit --no-verify -a -m 'chore: gh-pages' && git push origin gh-pages -f"
  },
  "dependencies": {
    "bootstrap": "4.0.0",
    "jquery": "3.3.1",
    "popper.js": "1.12.9",
    "slick-carousel": "1.8.1"
  },
  "devDependencies": {
    "husky": "0.14.3",
    "prettier": "1.10.2",
    "serve": "6.4.9"
  }
}
